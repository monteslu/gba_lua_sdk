tools/
build/
examples/**/build/
*.gtr
node_modules/
!roms/*.gtr
ports/**/build-debug/

# built ROMs + build logs are regenerated by `gtlua build --target gba`
*.gba
*.gba.build.log

# scratch / spike experiments (throwaway probes, not part of the SDK)
spike0/
