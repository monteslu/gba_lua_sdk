{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "gba-lua: Build",
      "detail": "Compile the open .lua to a .gba ROM (same name, same folder)",
      "type": "shell",
      "command": "node",
      "args": [
        "${workspaceFolder}/bin/gtlua.js",
        "build",
        "${file}",
        "-o",
        "${fileDirname}/${fileBasenameNoExtension}.gba"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "presentation": {
        "reveal": "always",
        "panel": "shared"
      },
      "problemMatcher": []
    }
  ]
}
