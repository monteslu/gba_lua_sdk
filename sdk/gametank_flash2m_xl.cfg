# GameTank FLASH2M "XL" ld65 config — the base FLASH2M layout plus extra 16 KB
# code banks (BANK4/BANK5). A 2 MB cart has 128 switchable banks; the base cfg
# only exposes 3 for game code + 1 for audio + the fixed bank, which is enough
# for most carts. Games whose code + banked SDK exceed that window (e.g.
# ports/celeste2 at the capacity cliff) use this cfg to spill cold functions
# into banks 4/5 — the hardware and gt_bank.s already address any bank 0-127.
# Layout is identical to gametank_flash2m.cfg: $8000-$BFFF is the switched
# window, $C000-$FFFF is fixed to bank 127. The builder places bank n at
# n*0x4000 in the 2 MB image and FIXED at 0x1FC000.
MEMORY {
    ZP:    start = $0000, size = $0100, type = rw, define = yes;
    RAM:   start = $0200, size = $1D00, define = yes;
    BANK0: start = $8000, size = $4000, file = %O, fill = yes, define = yes;
    BANK1: start = $8000, size = $4000, file = %O, fill = yes, define = yes;
    BANK2: start = $8000, size = $4000, file = %O, fill = yes, define = yes;
    BANK3: start = $8000, size = $4000, file = %O, fill = yes, define = yes;
    BANK4: start = $8000, size = $4000, file = %O, fill = yes, define = yes;
    BANK5: start = $8000, size = $4000, file = %O, fill = yes, define = yes;
    ROM:   start = $C000, size = $4000, file = %O, fill = yes, define = yes;
}
SEGMENTS {
    ZEROPAGE: load = ZP,  type = zp,  define = yes;
    DATA:     load = ROM, type = rw,  define = yes, run = RAM;
    BSS:      load = RAM, type = bss, define = yes;
    HEAP:     load = RAM, type = bss, optional = yes;
    STARTUP:  load = ROM, type = ro;
    ONCE:     load = ROM, type = ro,  optional = yes;
    CODE:     load = ROM, type = ro;
    RODATA:   load = ROM, type = ro;
    B0CODE:   load = BANK0, type = ro, optional = yes;
    B0RODATA: load = BANK0, type = ro, optional = yes;
    B1CODE:   load = BANK1, type = ro, optional = yes;
    B1RODATA: load = BANK1, type = ro, optional = yes;
    SHEET:    load = BANK2, type = ro, optional = yes;
    B2CODE:   load = BANK2, type = ro, optional = yes;
    B2RODATA: load = BANK2, type = ro, optional = yes;
    B3CODE:   load = BANK3, type = ro, optional = yes;
    B3RODATA: load = BANK3, type = ro, optional = yes;
    B4CODE:   load = BANK4, type = ro, optional = yes;
    B4RODATA: load = BANK4, type = ro, optional = yes;
    B5CODE:   load = BANK5, type = ro, optional = yes;
    B5RODATA: load = BANK5, type = ro, optional = yes;
    VECTORS:  load = ROM, type = ro,  start = $FFFA;
}
FEATURES {
    CONDES: type = constructor, label = __CONSTRUCTOR_TABLE__, count = __CONSTRUCTOR_COUNT__, segment = ONCE;
    CONDES: type = destructor,  label = __DESTRUCTOR_TABLE__,  count = __DESTRUCTOR_COUNT__,  segment = RODATA;
}
SYMBOLS {
    __STACKSIZE__: type = weak, value = $0200;
}
